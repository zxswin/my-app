## 单词

```pug
distinct 不同的,明显的
alter 改变
truncate 截断;截短，缩短，删节(尤指掐头或去尾)
character 性格 文字，字母，符号
general 全体的 常规的
decimal 十进制的;小数的;十进位的 小数
interval 间隔;(时间上的)间隙;间歇;
union 联合 协会
constraint 限制
foreign 外国的
references 关联 文献参考
cascade 串联 大量落下 小瀑布
```
