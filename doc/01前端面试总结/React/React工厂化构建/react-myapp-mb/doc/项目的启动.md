## 项目的启动

- 依赖安装

```js
// 依赖的安装
// cnpm i
```

- 解决 react 单页面运用切换路径后刷新页面无法找到页面的问题

```js
devServer:{
  // 解决react等单页面运用刷新页面后找不到页面的问题
    historyApiFallback: true,
}
```

- 解决移动端访问本地开发项目

```js
devServer:{
  // host 配置成 ‘0.0.0.0’, 充许本机以外的设备访问
  host: '0.0.0.0',
  // useLocalIp true, 将以内网IP为http地址信息打开浏览器, 不用敲 cmd ipinfo 去查内网IP了
  useLocalIp: true,
}
```

- 解决使用 mini-css-extract-plugin 单独抽取 css 文件后 修改 css 文件后页面不自动刷新的问题

```js
// 安装 css-hot-loader
// cnpm i css-hot-loader -D

// 解析scss文件
{
  test: /\.scss$/,
  use: [
    {
      loader: 'css-hot-loader',
    },
    {
      loader: MiniCssExtractPlugin.loader,
      options: {
        esModule: true,
      },
    },
    {
      loader: 'css-loader',
      options: {
        sourceMap: true,
        modules: {
          localIdentName: '[local]--[hash:base64:5]',
        },
      },
    },
    {
      loader: 'postcss-loader',
      options: {
        sourceMap: true,
        ident: 'postcss',
        plugins: [
          require('postcss-preset-env')({
            browsers: 'last 5 versions',
            autoprefixer: { grid: true },
          }),
          require('postcss-pxtorem')({
            rootValue: 75,
            propList: ['*'],
          }),
        ],
      },
    },
    {
      loader: 'sass-loader',
      options: {
        sourceMap: true,
      },
    },
  ],
  include: path.resolve(__dirname, 'src'),
},
```

- 让 scss 保持原有类名的方法

```scss
@import '../../../styles/variable.scss';

.ProdDetail {
  position: relative;
  padding: 30px;
  .swiper {
    :global(.swiper-pagination-bullet-active) {
      opacity: 1;
      background: $primary;
    }
  }
}
```
